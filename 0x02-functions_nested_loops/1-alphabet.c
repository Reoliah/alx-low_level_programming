#include "main.h"

/**
 * main - print all alphabets in lower case followed by a new line
 * Return: Always 0 (Success)
 */

void print_alphabet(void)
{
	int y;

	for (y = 'a'; y <= 'z'; a++)
	{_putchar(y);
	}
	_putchar('\n');
}
