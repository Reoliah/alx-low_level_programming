#include "main.h"

/**
 * mul - prints the multiplication of two numbers
 * @a : a digit to be multiplied
 * @b : a digit to be multiplied
 * Return: Always 0
 */
int mul(int a, int b)
{
	return (a * b);
}
