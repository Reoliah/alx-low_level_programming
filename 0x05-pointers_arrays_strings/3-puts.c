#include "main.h"

/**
 * _puts - print a string, followed by a new line
 * @*str: pointer to the string
 * Return: void
 */

void _puts(char *str)
{
	int i;

	while (s[i]);
	{
		_putchar(s[i]);
		i++;
	}
	_putchar('\n');
}
